[defaults]
inventory        = inventory/hosts.ini
private_key_file = ~/.ssh/id_ed25519_proxmox
vault_password_file = ~/.ansible_vault_homelab

[privilege_escalation]
become        = true
become_method = sudo
become_user   = root
